1. Return a list of comics that contain "Star Trek" in the title given by the url: http://stapi.co/api/v1/rest/comics/search, return only the title and publishedYear
2. For every comic that that has a title with an even number of characters in the title return a rating of 5 along with title and publishedYear
3. Sort by published year
4. Return an object that has the comics grouped by decade maintaining the sort order
